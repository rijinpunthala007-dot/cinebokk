# Core Django
Django==5.1.4
djangorestframework==3.15.2

# Auth / JWT
djangorestframework-simplejwt==5.3.1

# CORS
django-cors-headers==4.4.0

# Environment / secrets
django-environ==0.11.2

# Static file serving (production — WhiteNoise serves collected static via WSGI)
whitenoise==6.7.0

# Database drivers
mysqlclient==2.2.4          # local dev (MySQL)
psycopg[binary]==3.2.3      # production (PostgreSQL on Render) — psycopg3, broad wheel support 3.12–3.14
dj-database-url==2.2.0      # parse Render's DATABASE_URL

# WSGI server (production)
gunicorn==23.0.0

# Cron (Celery-free background sweeper)
django-crontab==0.7.1

# Image handling (for poster uploads)
Pillow==10.4.0

# Testing utilities
factory-boy==3.3.1
